<section class="section product-reviews-section" id="product-reviews">
  <div class="wrap">
    <h2 class="product-reviews__title">{{ section.settings.heading }}</h2>
    {%- for block in section.blocks -%}
      {%- if block.type == '@app' -%}
        {%- render block -%}
      {%- endif -%}
    {%- endfor -%}
    <div id="shopify-product-reviews" data-id="{{ product.id }}">{{ product.metafields.spr.reviews }}</div>
  </div>
</section>
{% schema %}
{
  "name": "Reviews app",
  "settings": [
    {"type":"text","id":"heading","label":"Titular","default":"Customer Reviews"}
  ],
  "blocks": [{"type": "@app"}],
  "presets": [{"name": "Reviews app"}]
}
{% endschema %}
